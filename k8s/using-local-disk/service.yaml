apiVersion: v1
kind: Service
metadata:
  name: postgres-agents-svc
  namespace: pg-agents
  labels:
    app: postgres
    environment: dev
spec:
  type: LoadBalancer
  selector:
    app: postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
      name: postgres
